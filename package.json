{
  "name": "rename-massive-gui",
  "version": "1.0.0",
  "description": "GUI for massive file rename using Electron",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . rename-massive-gui --platform=win32 --arch=x64 --out=dist"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^43.1.1"
  }
}
